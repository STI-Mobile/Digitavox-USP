<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Digitavox.ViewModels"
             x:Class="Digitavox.Views.TutorialView"
             x:DataType="viewModels:TutorialViewModel">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="False" IsEnabled="False"/>
    </Shell.BackButtonBehavior>

    <ScrollView AutomationProperties.ExcludedWithChildren="True">
        <VerticalStackLayout>
            <Label FormattedText = "{Binding PageFormattedLabel}"
            FontSize="{Binding TextSize}"
            Padding="10,10,5,20"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
